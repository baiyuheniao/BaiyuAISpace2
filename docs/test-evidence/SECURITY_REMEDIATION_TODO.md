# 安全修复 Todo

本清单用于跟踪 2026-08-01 安全测试后的修复工作；详细漏洞证据保留在私密安全报告中，不应公开提交。

- [x] P0 密钥隔离：前端不再读取或携带 API Key；Rust 仅按配置 ID 从系统凭据管理器取用。
- [x] P0 URL 抓取边界：默认禁止本机与内网目标；设置页提供明确、持久化的可选开关；拒绝时显示统一左下角提示。
- [x] P0 Skill 文件边界：拒绝非 UUID 的 Skill ID、绝对路径、路径分隔符与 `..`；删除前验证目标 Skill 存在。
- [ ] P1 工具授权：建立私密跟踪记录，后续实现按能力和参数的统一后端授权与审计。
- [ ] P2 Tauri capabilities 最小化：在完整调用审计后单独实施，避免误伤更新、文件对话框等正常功能。

## 验收原则

- 现有聊天、模型配置、Skill 资源管理与公网网页抓取可继续使用。
- 高风险目标在后端拒绝，不能仅依赖前端隐藏按钮。
- 每项完成后运行 `pnpm build`、`cargo build`，并使用隔离靶场做动态回归验证。
