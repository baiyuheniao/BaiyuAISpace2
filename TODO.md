# TODO

## 插件系统

现有的功能扩展点（MCP、Skill）都是应用内置的固定形态，第三方开发者没有更底层的接入方式。
考虑做一套插件系统，让开发者能给应用扩展新能力（自定义工具、自定义 UI 面板等），而不只是
通过 MCP 协议接入外部工具。

需要想清楚的点：
- 插件加载机制（本地目录扫描 / 手动导入包），要不要签名校验
- 权限沙箱：插件可能需要访问文件系统、网络，怎么限制范围、怎么向用户展示"这个插件能做什么"
- 和现有 MCP/Skill 体系的边界——插件是否只是"给开发者的更底层扩展点"，普通用户仍然只接触
  MCP 市场/Skill 这类高层封装

## 新手引导

目前首次打开应用没有任何引导流程，新用户直接看到空的对话页（`ChatView.vue` 提示"请先前往
设置创建 API 配置"），需要自己摸索"先去设置配 API"这类前置知识，也不知道 MCP/Skill/Agent
Team 这些概念是什么、什么时候该用。

考虑做一个首次启动的分步引导，覆盖：配置 API → 简单介绍 MCP/Skill/Agent Team 各自是什么、
分别解决什么问题 → 引导发出第一条消息。引导本身的视觉风格要遵循黑白编辑设计系统（见
`design-system` skill），不能用高亮色块这类和整体调性冲突的常见引导库样式。

## 前端布局/交互重构

背景：前端长期由 Agent 编写，用户只交代了艺术设计规范（黑白单色），但"按钮放哪、是下拉还是
二级页、某个操作在哪个页面做"这类布局与交互流从没被钉死，Agent 各凭感觉填，结果和用户想象
错位。直接重构代码解决不了这个——问题不在代码，在规范没钉死，重写完还是会漂。

方法：
- 对每个页面先出"现状清单"（控件位置/行为），用户对着标"对/错/挪到哪"
- 用户标注最高效是直接在截图上画；改的过程中把反复出现的规则沉淀成一份"布局/交互约定"
- 按"最常用 / 最不顺眼"排序，一页一页改，不做全量重写

进度：
- [x] Chat 页（2026-08-27）：右侧会话上下文面板（文件/知识库/MCP/Skill）、设置项（消息内容
  宽度、输入框聚焦上浮、消息体边框、头像、显示名）、思考按钮按服务商支持显示/隐藏、消息用量
  埋进输入区底部、移除侧边栏 Monochrome 字样；测试截图见 docs/test-evidence/refactor-*.png
- [ ] 其余页面（技能/知识库/MCP/本地部署/协作团队/定时任务/迁移中心/历史…）按同一流程走

后续待办：
- "统计面板"：用量展示从 Chat 挪走后，单独做成一个独立模块
- 思考按钮"隐藏"逻辑：目前只实测了支持思考的服务商（siliconflow），需用非思考 provider（如
  OpenAI/Mistral）实机确认会隐藏
